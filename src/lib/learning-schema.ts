import type { LearningLocale } from "@/src/data/grade9";
export const tutorModes = ["ask", "simplify", "analogy", "example", "quiz", "diagnose"] as const;
export type TutorMode = typeof tutorModes[number];
export type TutorRequest = { locale: LearningLocale; topicId: string; mode: TutorMode; question?: string; userAnswer?: string; history?: Array<{ role: "user" | "model"; text: string }> };
export type TutorResponse = { answer: string; steps: string[]; keyInsight: string; followUpQuestion?: string; quiz?: { question: string; options?: string[]; answer?: string } };
export function parseTutorRequest(value: unknown): TutorRequest | undefined { if (!value || typeof value !== "object") return; const input = value as Record<string, unknown>; if (!(["zh", "en", "ja"] as unknown[]).includes(input.locale) || typeof input.topicId !== "string" || input.topicId.length > 100 || !tutorModes.includes(input.mode as TutorMode)) return; if (typeof input.question === "string" && input.question.length > 1000 || typeof input.userAnswer === "string" && input.userAnswer.length > 1000) return; if (input.history !== undefined && (!Array.isArray(input.history) || input.history.length > 8 || input.history.some((x) => !x || !["user", "model"].includes(x.role) || typeof x.text !== "string" || x.text.length > 1000))) return; return input as TutorRequest; }
export function parseTutorResponse(value: unknown): TutorResponse | undefined { if (!value || typeof value !== "object") return; const x = value as Record<string, unknown>; if (typeof x.answer !== "string" || !x.answer.trim() || !Array.isArray(x.steps) || x.steps.some((s) => typeof s !== "string") || typeof x.keyInsight !== "string") return; return x as TutorResponse; }
