import type { Topic } from "@/src/data/grade9"; import type { Locale } from "@/src/config/site"; import { localize } from "@/src/lib/grade9-curriculum"; import { QuickCheck } from "./QuickCheck"; import { LearningProgress } from "./LearningProgress";
export function TenSecondLesson({ topic, locale }: { topic: Topic; locale: Locale }) { return <div className="space-y-5"><section className="rounded-3xl border border-cyan-300/30 bg-gradient-to-br from-cyan-950 to-slate-900 p-6 sm:p-8"><p className="text-sm font-bold uppercase tracking-widest text-cyan-300">10 秒理解</p><h1 className="mt-3 text-3xl font-black sm:text-4xl">{localize(topic.title, locale)}</h1><p className="mt-5 text-xl font-semibold leading-relaxed">{localize(topic.tenSecondSummary, locale)}</p><div className="mt-6 rounded-2xl bg-slate-950/60 p-4"><strong>生活类比</strong><p className="mt-2 text-slate-300">{localize(topic.analogy, locale)}</p></div><h2 className="mt-6 font-bold">三个核心点</h2><ul className="mt-2 list-disc space-y-2 pl-5 text-slate-200">{topic.keyPoints.slice(0, 3).map((point) => <li key={point.zh}>{localize(point, locale)}</li>)}</ul>{topic.formula && <p className="mt-5 overflow-x-auto rounded-xl bg-white/10 p-4 font-mono text-lg" aria-label="公式">{topic.formula}</p>}</section><section className="grid gap-4 sm:grid-cols-2"><div className="rounded-2xl bg-slate-900 p-5"><h2 className="font-bold">方法步骤</h2><ol className="mt-3 list-decimal space-y-2 pl-5">{topic.methodSteps.map((step) => <li key={step.zh}>{localize(step, locale)}</li>)}</ol></div><div className="rounded-2xl bg-slate-900 p-5"><h2 className="font-bold">典型例子</h2><p className="mt-3 text-slate-300">{topic.workedExample && localize(topic.workedExample, locale)}</p><h2 className="mt-5 font-bold text-rose-300">最常见错误</h2><p className="mt-2">{localize(topic.commonMistakes[0], locale)}</p></div></section><QuickCheck topic={topic} locale={locale} /><LearningProgress topicId={topic.id} /></div> }
