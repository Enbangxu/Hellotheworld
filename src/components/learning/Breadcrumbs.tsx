import Link from "next/link";
export function Breadcrumbs({ items }: { items: Array<{ label: string; href?: string }> }) { return <nav aria-label="Breadcrumb" className="mb-6 text-sm text-slate-400"><ol className="flex flex-wrap gap-2">{items.map((item, index) => <li key={`${item.label}-${index}`} className="flex gap-2">{index > 0 && <span aria-hidden>›</span>}{item.href ? <Link className="hover:text-white focus-visible:outline focus-visible:outline-2" href={item.href}>{item.label}</Link> : <span aria-current="page" className="text-slate-200">{item.label}</span>}</li>)}</ol></nav> }
