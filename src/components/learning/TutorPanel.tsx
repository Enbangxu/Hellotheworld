"use client";
import { useState } from "react"; import type { Locale } from "@/src/config/site"; import type { TutorMode, TutorResponse } from "@/src/lib/learning-schema";
const actions: Array<[TutorMode, string]> = [["simplify", "再简单一点"], ["analogy", "换个生活类比"], ["example", "给我一道例题"], ["ask", "用提问引导我"], ["quiz", "生成一道新题"], ["diagnose", "检查我的答案"]];
export function TutorPanel({ topicId, locale }: { topicId: string; locale: Locale }) { const [question, setQuestion] = useState(""); const [result, setResult] = useState<TutorResponse>(); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); const run = async (mode: TutorMode) => { setLoading(true); setError(""); setResult(undefined); try { const response = await fetch("/api/learning/tutor", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ locale, topicId, mode, question, userAnswer: mode === "diagnose" ? question : undefined }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error?.message); setResult(data); } catch (e) { setError(e instanceof Error ? e.message : "AI 导师暂时不可用。"); } finally { setLoading(false); } }; return <aside className="rounded-3xl border border-violet-400/30 bg-slate-900 p-5 sm:p-6" aria-labelledby="tutor-title"><p className="text-xs font-bold text-violet-300">AI 导师由 Gemini 2.5 Flash 提供</p><h2 id="tutor-title" className="mt-2 text-2xl font-black">继续追问</h2><textarea value={question} onChange={(e) => setQuestion(e.target.value)} maxLength={1000} aria-label="输入问题或答案" placeholder="输入你的问题或答案…" className="mt-4 min-h-24 w-full rounded-xl border border-white/20 bg-slate-950 p-3 focus-visible:outline focus-visible:outline-2 focus-visible:outline-violet-300"/><div className="mt-3 grid grid-cols-2 gap-2">{actions.map(([mode, label]) => <button disabled={loading} type="button" key={mode} onClick={() => run(mode)} className="rounded-xl bg-violet-500/20 p-3 text-sm font-bold hover:bg-violet-500/30 disabled:opacity-50">{label}</button>)}</div>{loading && <p role="status" className="mt-4 text-slate-300">正在连接导师并整理答案…</p>}{error && <p role="alert" className="mt-4 rounded-xl bg-amber-950 p-3 text-amber-200">{error}</p>}{result && <div className="mt-5 space-y-3" aria-live="polite"><p className="text-lg font-semibold">{result.answer}</p>{result.steps.length > 0 && <ol className="list-decimal pl-5">{result.steps.map((step) => <li key={step}>{step}</li>)}</ol>}<p className="rounded-xl bg-slate-950 p-3"><strong>关键：</strong>{result.keyInsight}</p>{result.followUpQuestion && <p>{result.followUpQuestion}</p>}</div>}</aside> }
