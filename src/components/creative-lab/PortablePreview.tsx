"use client";import{useEffect,useState}from"react";import Link from"next/link";import{decodePortable}from"@/src/lib/portable-share";import type{CreativeWork}from"@/src/lib/creative-schema";import{CreationResult}from"./CreationResult";export function PortablePreview(){const[work,setWork]=useState<CreativeWork|null>(null),[error,setError]=useState(false);useEffect(()=>{try{setWork(decodePortable(location.hash.slice(1)))}catch{setError(true)}},[]);return <main className="creative-lab min-h-screen bg-slate-950 px-5 py-10 text-white"><div className="mx-auto max-w-5xl">{error?<div role="alert" className="creative-glass p-8">这个便携预览链接无效或内容过长。 <Link href="/create" className="underline">返回实验室</Link></div>:work?<><CreationResult work={work} onRefine={()=>{}}/><Link href="/create" className="creative-primary mt-6">用这个灵感再创作</Link></>:<p aria-live="polite">正在安全解析预览…</p>}</div></main>}
