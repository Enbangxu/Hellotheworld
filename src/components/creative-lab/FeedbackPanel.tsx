import{useState}from"react";export function FeedbackPanel(){const [done,setDone]=useState(false);if(done)return <p className="creative-glass p-5">感谢反馈，不会打断你的创造。</p>;return <section className="creative-glass p-5"><h3 className="font-bold">这份作品有帮助吗？</h3><div className="mt-3 flex flex-wrap gap-2"><button className="creative-button" onClick={()=>setDone(true)}>有帮助</button>{["不够具体","不够有创意","不符合我的想法","内容太长","内容太短","其他"].map(x=><button key={x} className="creative-button" onClick={()=>setDone(true)}>{x}</button>)}</div></section>}
