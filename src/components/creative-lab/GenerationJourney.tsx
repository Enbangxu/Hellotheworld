import {Check,Circle,LoaderCircle,X} from "lucide-react";export function GenerationJourney({stages,active,onCancel,slow}:{stages:readonly string[];active:number;onCancel:()=>void;slow:boolean}){return <section aria-live="polite" className="creative-glass p-6"><h2 className="text-xl font-bold">AI 正在认真构建</h2><ol className="mt-5 space-y-4">{stages.map((s,i)=><li key={s} className="flex items-center gap-3">{i<active?<Check className="text-cyan-300"/>:i===active?<LoaderCircle className="animate-spin text-violet-300"/>:<Circle className="text-slate-600"/>}<span className={i<=active?"text-white":"text-slate-500"}>{s}</span></li>)}</ol>{slow&&<p className="mt-5 text-amber-200">仍在认真构建，请不要重复提交。</p>}<button onClick={onCancel} className="creative-button mt-5"><X size={17}/>取消</button></section>}
