import Link from "next/link";
import { FloatingBlobs } from "@/src/components/FloatingBlobs";
import type { Locale } from "@/src/config/site";

const links = [ ["lab", "AI Lab"], ["tools", "AI Tools"], ["assistant", "Assistant"], ["community", "Community"], ["knowledge", "Knowledge"], ["changelog", "Changelog"] ];
export function EcosystemPageShell({ locale, eyebrow, title, subtitle, children }: { locale: Locale; eyebrow: string; title: string; subtitle: string; children: React.ReactNode }) { return <main className="dark min-h-screen overflow-hidden"><div className="animated-gradient min-h-screen text-white"><FloatingBlobs /><div className="relative z-10 mx-auto max-w-6xl px-6 py-8"><nav className="mb-16 flex flex-wrap items-center justify-between gap-4 rounded-3xl border border-white/15 bg-slate-950/55 px-5 py-3 backdrop-blur-2xl"><Link href={`/${locale}`} className="text-lg font-black tracking-tight">Enbang AI Studio</Link><div className="flex flex-wrap gap-2 text-sm font-bold">{links.map(([href, label]) => <Link key={href} className="rounded-full bg-white/10 px-4 py-2 hover:bg-white/20" href={`/${locale}/${href}`}>{label}</Link>)}</div></nav><section className="mb-10 max-w-3xl"><p className="mb-4 text-sm font-black uppercase tracking-[0.35em] text-cyan-200">{eyebrow}</p><h1 className="text-4xl font-black tracking-tight md:text-6xl">{title}</h1><p className="mt-5 text-lg leading-8 text-slate-200">{subtitle}</p></section>{children}</div></div></main>; }
