import type { Metadata } from "next";
import { EcosystemPageShell } from "@/src/components/ecosystem/EcosystemPageShell";
import { getLocale } from "@/src/lib/i18n";
export const metadata: Metadata = { title: "Create an AI Agent", description: "Design and publish a useful AI agent in the Hello the World marketplace." };
export default async function CreateAgentPage({ params }: { params: Promise<{ locale: string }> }) { const { locale: raw } = await params; const locale = getLocale(raw); return <EcosystemPageShell locale={locale} eyebrow="Agent studio" title="Create an agent people love." subtitle="Define a clear job, give it a reliable prompt, and prepare it for the V10 marketplace."><form className="grid gap-5 rounded-[2rem] border border-white/15 bg-white/10 p-6 md:grid-cols-2"><Field label="Agent name" placeholder="Research Navigator"/><Field label="Category" placeholder="Knowledge"/><div className="md:col-span-2"><Field label="Description" placeholder="What does this agent help people accomplish?"/></div><div className="md:col-span-2"><label className="font-black" htmlFor="prompt">System prompt</label><textarea id="prompt" rows={7} placeholder="You are a focused assistant that…" className="mt-2 w-full rounded-2xl border border-white/10 bg-slate-950/40 p-4 outline-none focus:border-cyan-300"/></div><button type="button" className="w-fit rounded-full bg-cyan-300 px-6 py-3 font-black text-slate-950">Save agent draft</button></form></EcosystemPageShell>; }
function Field({ label, placeholder }: { label: string; placeholder: string }) { const id = label.toLowerCase().replace(" ", "-"); return <div><label className="font-black" htmlFor={id}>{label}</label><input id={id} placeholder={placeholder} className="mt-2 w-full rounded-2xl border border-white/10 bg-slate-950/40 px-4 py-3 outline-none focus:border-cyan-300"/></div>; }
