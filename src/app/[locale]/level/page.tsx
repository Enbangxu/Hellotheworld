import type { Metadata } from "next";
import { EcosystemPageShell } from "@/src/components/ecosystem/EcosystemPageShell";
import { type Locale } from "@/src/config/site";
import { levelBadges } from "@/src/data/ai-universe";
import { getLocale } from "@/src/lib/i18n";
export const metadata: Metadata = { title: "AI Universe Levels", description: "XP, levels, badges, and achievements for Enbang AI Universe V6." };
export default async function LevelPage({ params }: { params: Promise<{ locale: string }> }) { const { locale: rawLocale } = await params; const locale: Locale = getLocale(rawLocale); return <EcosystemPageShell locale={locale} eyebrow="Gamification" title={locale === "zh" ? "等级与成就" : locale === "ja" ? "レベルと実績" : "Levels & Achievements"} subtitle="Reward learning, publishing, collaboration, and community contribution with XP, levels, badges, and achievements."><div className="rounded-3xl border border-white/15 bg-white/10 p-6 backdrop-blur-xl"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-black uppercase tracking-[0.3em] text-cyan-200">User XP</p><h2 className="mt-2 text-5xl font-black">8,760 XP</h2></div><p className="rounded-full bg-cyan-300 px-5 py-2 font-black text-slate-950">Level 12</p></div><div className="mt-6 h-4 overflow-hidden rounded-full bg-white/10"><div className="h-full w-3/4 rounded-full bg-gradient-to-r from-cyan-300 to-fuchsia-300" /></div><div className="mt-8 grid gap-4 md:grid-cols-3">{levelBadges.map((badge, index) => <div key={badge} className="rounded-2xl bg-slate-950/40 p-5"><p className="text-3xl">🏆</p><h3 className="mt-3 font-black">{badge}</h3><p className="mt-2 text-sm text-slate-300">Achievement #{index + 1}</p></div>)}</div></div></EcosystemPageShell>; }
