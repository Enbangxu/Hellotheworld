import type { Metadata } from "next"; import Link from "next/link"; import { getLocale } from "@/src/lib/i18n"; import { SubjectGrid } from "@/src/components/learning/SubjectGrid"; import { Breadcrumbs } from "@/src/components/learning/Breadcrumbs"; import { LearningSearch } from "@/src/components/learning/LearningSearch";
export const metadata: Metadata = { title: "九年级上册七科学习目录", description: "中国大陆通用中考课程框架，七科章节与知识点目录。" };
export default async function Page({ params }: { params: Promise<{ locale: string }> }) { const locale = getLocale((await params).locale); return <main className="min-h-screen bg-slate-950 px-5 py-14 text-white"><div className="mx-auto max-w-6xl"><Breadcrumbs items={[{label:"学习中心",href:`/${locale}/knowledge`},{label:"九年级上册"}]}/><div className="flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-4xl font-black">九年级上册 · 通用版</h1><p className="mt-3 text-slate-300">选择学科，按章节逐层学习。课程目录和核心解释均为静态内容。</p></div><Link href={`/${locale}/knowledge/grade-9/progress`} className="rounded-xl bg-cyan-300 px-5 py-3 font-bold text-slate-950">学习进度 →</Link></div><div className="mt-8"><SubjectGrid locale={locale}/></div><LearningSearch locale={locale}/></div></main> }
