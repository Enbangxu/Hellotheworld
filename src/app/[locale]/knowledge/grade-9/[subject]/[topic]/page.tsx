import type { Metadata } from "next"; import Link from "next/link"; import { notFound } from "next/navigation"; import { allTopics, getSubject, getTopicBySlug, localize } from "@/src/lib/grade9-curriculum"; import { getLocale } from "@/src/lib/i18n"; import { Breadcrumbs } from "@/src/components/learning/Breadcrumbs"; import { TenSecondLesson } from "@/src/components/learning/TenSecondLesson"; import { TutorPanel } from "@/src/components/learning/TutorPanel";
export function generateStaticParams() { return allTopics().map(({subject,topic}) => ({subject:subject.slug,topic:topic.slug})); }
export async function generateMetadata({params}:{params:Promise<{locale:string;subject:string;topic:string}>}):Promise<Metadata>{const p=await params,locale=getLocale(p.locale),subject=getSubject(p.subject),topic=getTopicBySlug(p.subject,p.topic);if(!subject||!topic)return{};const path=`/knowledge/grade-9/${p.subject}/${p.topic}`;return{title:`${localize(topic.title,locale)}｜10 秒理解`,description:localize(topic.tenSecondSummary,locale),alternates:{canonical:`/${locale}${path}`,languages:{zh:`/zh${path}`,en:`/en${path}`,ja:`/ja${path}`}}};}
export default async function Page({params}:{params:Promise<{locale:string;subject:string;topic:string}>}){const p=await params,locale=getLocale(p.locale),subject=getSubject(p.subject),topic=getTopicBySlug(p.subject,p.topic);if(!subject||!topic)notFound();const list=subject.chapters.flatMap(c=>c.topics),index=list.findIndex(t=>t.id===topic.id),previous=list[index-1],next=list[index+1],base=`/${locale}/knowledge/grade-9/${subject.slug}`;return <main className="min-h-screen bg-slate-950 px-5 py-12 text-white"><div className="mx-auto max-w-6xl"><Link href={`/${locale}/knowledge/grade-9/progress`} className="float-right rounded-lg border border-cyan-300 px-3 py-2 text-sm font-bold text-cyan-200">学习进度 →</Link><Breadcrumbs items={[{label:"学习中心",href:`/${locale}/knowledge`},{label:localize(subject.name,locale),href:base},{label:localize(topic.title,locale)}]}/><div className="grid items-start gap-7 lg:grid-cols-[minmax(0,1fr)_380px]"><TenSecondLesson topic={topic} locale={locale}/><TutorPanel topicId={topic.id} locale={locale}/></div><nav aria-label="知识点导航" className="mt-10 grid gap-3 sm:grid-cols-3"><Link href={base} className="rounded-xl bg-slate-900 p-4 text-center">返回学科页</Link>{previous?<Link href={`${base}/${previous.slug}`} className="rounded-xl bg-slate-900 p-4">← {localize(previous.title,locale)}</Link>:<span/>}{next&&<Link href={`${base}/${next.slug}`} className="rounded-xl bg-slate-900 p-4 text-right">{localize(next.title,locale)} →</Link>}</nav></div></main>}
