import type { Metadata } from "next";
import { EcosystemPageShell } from "@/src/components/ecosystem/EcosystemPageShell";
import { type Locale } from "@/src/config/site";
import { getLocale } from "@/src/lib/i18n";
export const metadata: Metadata = { title: "AI Assistant", description: "Personalized recommendations, user context, project suggestions, and RAG-ready chat UI." };
export default async function AssistantPage({ params }: { params: Promise<{ locale: string }> }) { const { locale: rawLocale } = await params; const locale: Locale = getLocale(rawLocale); const suggestions = ["Turn saved prompts into a marketplace agent", "Connect knowledge files to a RAG experiment", "Share your workspace with a creator mentor"]; return <EcosystemPageShell locale={locale} eyebrow="AI Assistant" title={locale === "zh" ? "AI 助手入口" : locale === "ja" ? "AIアシスタント" : "AI Assistant"} subtitle="A personalized assistant surface for recommendations, user context, project suggestions, OpenAI API, RAG retrieval, and vector database memory."><div className="grid gap-6 lg:grid-cols-[1.4fr_1fr]"><div className="rounded-3xl border border-white/15 bg-white/10 p-4 backdrop-blur-xl"><div className="space-y-4 rounded-2xl bg-slate-950/45 p-4"><div className="max-w-[85%] rounded-2xl bg-white/10 p-4"><p className="text-sm font-black text-cyan-200">Assistant</p><p className="mt-2">Hello! Based on your AI Universe context, I recommend packaging your strongest prompt as an agent and linking it to a workspace knowledge file.</p></div><div className="ml-auto max-w-[85%] rounded-2xl bg-cyan-300 p-4 text-slate-950"><p className="text-sm font-black">You</p><p className="mt-2">Help me build ideas with AI.</p></div></div><form className="mt-4 flex gap-3"><input className="min-w-0 flex-1 rounded-2xl border border-white/15 bg-slate-950/50 px-4 py-3 font-semibold text-white outline-none" placeholder="Ask with project context..." /><button className="rounded-2xl bg-white px-5 py-3 font-black text-slate-950" type="button">Send</button></form></div><aside className="space-y-4"><section className="rounded-3xl border border-white/15 bg-white/10 p-6 backdrop-blur-xl"><h2 className="text-2xl font-black">User context</h2><p className="mt-3 text-slate-200">Active project: Agent Marketplace. Preferred languages: English, 中文, 日本語. Goal: publish reusable AI workflows.</p></section><section className="rounded-3xl border border-white/15 bg-white/10 p-6 backdrop-blur-xl"><h2 className="text-2xl font-black">Project suggestions</h2><ul className="mt-3 space-y-2 text-slate-200">{suggestions.map((item) => <li key={item}>• {item}</li>)}</ul></section></aside></div></EcosystemPageShell>; }
