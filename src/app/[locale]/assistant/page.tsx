import type { Metadata } from "next";
import { EcosystemPageShell } from "@/src/components/ecosystem/EcosystemPageShell";
import { type Locale } from "@/src/config/site";
import { getLocale } from "@/src/lib/i18n";
export const metadata: Metadata = { title: "AI Assistant", description: "Chat UI prepared for OpenAI API, RAG, and vector database integrations." };
export default async function AssistantPage({ params }: { params: Promise<{ locale: string }> }) { const { locale: rawLocale } = await params; const locale: Locale = getLocale(rawLocale); return <EcosystemPageShell locale={locale} eyebrow="AI Assistant" title={locale === "zh" ? "AI 助手入口" : locale === "ja" ? "AIアシスタント" : "AI Assistant"} subtitle="UI-first chat interface prepared for OpenAI API, RAG retrieval, and vector database memory."><div className="mx-auto max-w-3xl rounded-3xl border border-white/15 bg-white/10 p-4 backdrop-blur-xl"><div className="space-y-4 rounded-2xl bg-slate-950/45 p-4"><div className="max-w-[85%] rounded-2xl bg-white/10 p-4"><p className="text-sm font-black text-cyan-200">Assistant</p><p className="mt-2">Hello! I can help plan AI projects, compare tools, and turn knowledge into workflows. Backend integration is reserved for OpenAI API + RAG + Vector Database.</p></div><div className="ml-auto max-w-[85%] rounded-2xl bg-cyan-300 p-4 text-slate-950"><p className="text-sm font-black">You</p><p className="mt-2">Help me build ideas with AI.</p></div></div><form className="mt-4 flex gap-3"><input className="min-w-0 flex-1 rounded-2xl border border-white/15 bg-slate-950/50 px-4 py-3 font-semibold text-white outline-none" placeholder="Ask the AI Studio assistant..." /><button className="rounded-2xl bg-white px-5 py-3 font-black text-slate-950" type="button">Send</button></form><div className="mt-4 grid gap-3 text-sm text-slate-200 md:grid-cols-3"><p>OpenAI API adapter</p><p>RAG retrieval layer</p><p>Vector database memory</p></div></div></EcosystemPageShell>; }
