import type { Metadata } from "next";
import { Heart, MessageCircle, TrendingUp } from "lucide-react";
import { EcosystemPageShell } from "@/src/components/ecosystem/EcosystemPageShell";
import { type Locale } from "@/src/config/site";
import { getLocale } from "@/src/lib/i18n";
export const metadata: Metadata = { title: "Community Feed", description: "Feed, likes, comments, and trending discussions for the V7 AI Creator Network." };
export default async function CommunityPage({ params }: { params: Promise<{ locale: string }> }) { const { locale: rawLocale } = await params; const locale: Locale = getLocale(rawLocale); const posts = ["Weekly AI build jam: ship one agent in public", "Show your RAG workflow and get peer feedback", "Trending: creator marketplaces for niche assistants"]; return <EcosystemPageShell locale={locale} eyebrow="Community" title={locale === "zh" ? "创作者社区 Feed" : "Creator community feed"} subtitle="Like, comment, follow trends, and discover collaboration signals across the AI Creator Network."><div className="grid gap-5 lg:grid-cols-[1.3fr_0.7fr]"><section className="space-y-4">{posts.map((post, index) => <article key={post} className="rounded-3xl border border-white/15 bg-white/10 p-6 backdrop-blur-xl"><p className="text-sm font-black uppercase tracking-[0.25em] text-cyan-200">Feed #{index + 1}</p><h2 className="mt-3 text-2xl font-black">{post}</h2><div className="mt-5 flex gap-3"><button className="rounded-full bg-white/10 px-4 py-2 font-black"><Heart className="mr-2 inline" size={16} />Like</button><button className="rounded-full bg-white/10 px-4 py-2 font-black"><MessageCircle className="mr-2 inline" size={16} />Comment</button></div></article>)}</section><aside className="rounded-3xl border border-white/15 bg-white/10 p-6 backdrop-blur-xl"><TrendingUp className="text-cyan-200" /><h2 className="mt-3 text-2xl font-black">Trending</h2><p className="mt-3 text-slate-200">Agent launches, AI portfolios, micro-SaaS workflows, and multilingual creator distribution are rising this week.</p></aside></div></EcosystemPageShell>; }
