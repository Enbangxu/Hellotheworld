import type { Metadata } from "next";
import { Bot, Rocket, Send } from "lucide-react";
import { V7Shell } from "@/src/components/V7Shell";
import { Card, CardTitle } from "@/src/components/ui/card";
export const metadata: Metadata = { title: "AI Studio | Hello the world V7", description: "Create agents, projects, and publish AI-native products." };
export default function StudioPage() { const flows = [{ icon: Bot, title: "Create Agent", body: "Define persona, prompt, tools, category, pricing, and launch channel." }, { icon: Rocket, title: "Create Project", body: "Turn experiments into public projects with stack, status, collaborators, and assets." }, { icon: Send, title: "Publish", body: "Package agents, posts, demos, and marketplace listings in one release lane." }]; return <V7Shell eyebrow="AI Studio" title="Build, package, and publish AI agents" subtitle="The V7 studio adds a creator-first production pipeline for agents, projects, and launch content."><div className="grid gap-5 md:grid-cols-3">{flows.map(({ icon: Icon, title, body }) => <Card key={title} className="transition hover:-translate-y-2 hover:bg-white/15"><Icon className="text-cyan-200" size={38} /><CardTitle className="mt-5">{title}</CardTitle><p className="mt-3 text-slate-200">{body}</p><textarea className="mt-5 min-h-28 w-full rounded-2xl border border-white/15 bg-slate-950/45 p-4 text-white outline-none focus:ring-2 focus:ring-cyan-300" placeholder={`${title} brief...`} /></Card>)}</div></V7Shell>; }
