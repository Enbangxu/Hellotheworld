import type{Metadata}from"next";import Link from"next/link";import{notFound}from"next/navigation";import{prisma}from"@/src/lib/prisma";import{normalizeCreativeWork}from"@/src/lib/creative-schema";import{CreationResult}from"@/src/components/creative-lab/CreationResult";export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params,c=await prisma.creation.findFirst({where:{slug,isPublic:true}}).catch(()=>null);if(!c)return{robots:{index:false}};return{title:c.title,description:c.tagline,alternates:{canonical:`/creation/${slug}`},openGraph:{title:c.title,description:c.tagline}}}export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params,c=await prisma.creation.findFirst({where:{slug,isPublic:true}}).catch(()=>null);if(!c)notFound();const work=normalizeCreativeWork(c.result);return <main className="creative-lab min-h-screen bg-slate-950 px-5 py-10 text-white"><div className="mx-auto max-w-5xl space-y-6"><p className="text-sm text-slate-400">创建于 {c.createdAt.toLocaleDateString()}</p><CreationResult work={work} onRefine={()=>{}}/><div className="flex flex-wrap gap-3"><Link className="creative-primary" href={`/create?idea=${encodeURIComponent(c.input)}`}>用这个灵感再创作</Link><Link className="creative-button" href="/create">返回 AI 创造实验室</Link></div></div></main>}
