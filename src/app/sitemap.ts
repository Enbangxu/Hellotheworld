import type { MetadataRoute } from "next";
import { siteConfig } from "@/src/config/site";
import { projects } from "@/src/data/projects";
const routes = ["", "/space", "/memory", "/agents", "/agents/create", "/feed", "/creator", "/dashboard", "/marketplace", "/lab", "/tools", "/assistant", "/community", "/knowledge", "/changelog", "/global"];
export default function sitemap(): MetadataRoute.Sitemap { const projectRoutes = projects.map((project) => `/lab/${project.id}`); const localized=siteConfig.locales.flatMap((locale) => [...routes, ...projectRoutes].map((route) => ({ url: `${siteConfig.url}/${locale}${route}`, lastModified: new Date(), changeFrequency: "weekly" as const, priority: route === "" && locale === siteConfig.defaultLocale ? 1 : 0.8, alternates: { languages: Object.fromEntries([...siteConfig.locales.map((item) => [item, `${siteConfig.url}/${item}${route}`]), ["x-default", `${siteConfig.url}/en${route}`]]) } }))); return [...localized,{url:`${siteConfig.url}/discover`,lastModified:new Date(),changeFrequency:"daily",priority:.9},{url:`${siteConfig.url}/create`,lastModified:new Date(),changeFrequency:"weekly",priority:.9}]; }
